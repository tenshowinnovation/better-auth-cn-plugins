# Better Auth CN Plugins

Better Auth OAuth provider helpers for WeChat and Douyin.

## Package

This monorepo publishes one package:

```sh
pnpm add @tenshowinnovation/better-auth-cn-plugins
```

See the package README at `packages/better-auth-cn-plugins/README.md`.
